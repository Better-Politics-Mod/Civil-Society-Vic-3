ciso_B = {
	value = 500
}

ciso_size_scaling_factor = {
	value = 10000
}

ciso_state_atmosphere_value = {
	value = 0 
	if = {
		limit = {
			has_variable = ciso_state_atmosphere_value
		}
		add = var:ciso_state_atmosphere_value
	}
}

ciso_total_government_investment_w = {
	value = ciso_total_government_investment
	multiply = modifier:state_ciso_gov_investment_efficiency_add
}

ciso_suppression_cost_mult = {
	value = { value = 1 desc = "ciso_base_suppression" format = "ciso_percent_add_format"}
	add = {
		value = modifier:state_ciso_suppression_efficiency_add
		desc = "ciso_base_suppression_efficiency"
		format = "ciso_percent_add_format"
	}
	# todo
	add = {
		value = modifier:state_ciso_suppression_efficiency_illegal_add
		desc = "ciso_base_suppression_efficiency_illegal"
		format = "ciso_percent_add_format"
	}
}