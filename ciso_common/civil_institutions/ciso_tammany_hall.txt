ciso_ci_tammany_hall = {
	icon = "gfx/interface/icons/civil_institution_icons/tammany_hall.dds"

	visible = {
		state_region = s:STATE_NEW_YORK
	}

	possible = {
		always = yes
	}

	measure_weights = {
		value = 0
		if = {
			limit = {
				scope:measure = flag:ciso_ms_soup_kitchen
			}
			add = 1
		}
	}

	is_radical = {
		OWNER = {
			OR = {
				has_law = law_type:law_censorship
				has_law = law_type:law_outlawed_dissent
			}
		}
	}

	is_loyalist = {
		OWNER = {
			has_law = law_type:law_guaranteed_liberties
		}
	}

	social_impact = {
		value = organization # %
		multiply = organization
		multiply = organization
		divide = 10000 # organization is 1-100 so we need to divide by 100^2 (10,000) to get proper scaling
		multiply = size # the numbers of pops supporting the institution
		divide = ciso_size_scaling_factor # usu. 100,000
		multiply = 80
		round = yes
	}

	# multiplies organization trend change by x10
	organization_trend_mult = {
		value = 10
	}
}